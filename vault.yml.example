---
# Ansible Vault Example
# This file should be encrypted with: ansible-vault encrypt vault.yml
# To edit: ansible-vault edit vault.yml
# To decrypt: ansible-vault decrypt vault.yml

# Database credentials
vault_db_password: "your-secure-database-password-here"

# Backup encryption (optional)
vault_backup_encryption_key: "your-backup-encryption-key"

# API keys (if needed)
vault_api_key: "your-api-key"

# SSH keys (if needed)
vault_ssh_private_key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  your-private-key-content-here
  -----END OPENSSH PRIVATE KEY-----
